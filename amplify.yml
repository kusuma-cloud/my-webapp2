version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build needed for static HTML site"
    build:
      commands:
        - echo "Ready to deploy static site"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []